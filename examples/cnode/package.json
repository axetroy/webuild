{
  "name": "cnode",
  "version": "1.0.0",
  "description": "Example for webuild",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "webuild dev --src ./src --output ./dist",
    "build": "webuild build --src ./src --output ./dist"
  },
  "author": "Axetroy",
  "license": "Apache",
  "devDependencies": {
    "@axetroy/webuild": "^0.5.1"
  },
  "dependencies": {
    "timeago.js": "^3.0.2",
    "wxapp-r2": "^0.0.1"
  }
}
